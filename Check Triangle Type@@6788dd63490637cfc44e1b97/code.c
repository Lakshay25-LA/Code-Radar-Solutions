// Your code here...
#include <stdio.h>
int main() {

    int s1,s2,s3 ;

    scanf("%d %d %d", &s1, &s2, &s3);

    if (a == b && b == c){
        printf("Equilateral");
    }
    else if (a == b || b == c || a == c) {
        printf("Isosceles");
        
    }
    else (
        printf("Scalene");
    )
    return 0;
}