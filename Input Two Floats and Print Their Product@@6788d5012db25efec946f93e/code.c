#include <stdio.h>

int main() {
    float a, b;

    // Read two space-separated floating-point numbers
    scanf("%f %f", &a, &b);

    // Print their product
    printf("%.2f\n", a * b);

    return 0;
}
